import './HeroBackground.css'

/**
 * Page 1 — Hero background
 * Deep teal (#1dc2b1) at the edges, lightening radially toward center.
 * A tall white ellipse sits slightly below center — the product pedestal.
 */
export function HeroBackground() {
  return (
    <div className="hero-bg" aria-hidden="true" />
  )
}
